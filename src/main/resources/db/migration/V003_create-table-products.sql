CREATE TABLE products (
                    id BIGINT PRIMARY KEY AUTO_INCREMENT,
                    name varchar (255),
                    quantity int,
                    price Decimal(10, 2),
                    description varchar(255)

);